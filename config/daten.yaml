# Datenquelle - wähle eine:

# Option 1: Lokale JSON-Datei (für Entwicklung)
# Projekt-spezifisch: pilze.json für Funginomi
quelle:
  typ: json
  url: ./data/pilze.json

# Option 2: PocketBase
# quelle:
#   typ: pocketbase
#   url: https://api.example.com
#   sammlung: items

# Option 3: REST API
# quelle:
#   typ: rest
#   url: https://api.example.com/items
#   headers:
#     Authorization: Bearer ${API_TOKEN}

# Schema für Validierung wird aus schema.yaml geladen!
# Keine Duplikation mehr nötig
