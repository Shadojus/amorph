# Schema-Definition
# Passe diese Datei an deine Datenstruktur an

# === META-KONFIGURATION ===
# Welche Felder haben spezielle Bedeutung?
meta:
  nameField: titel         # Welches Feld ist der angezeigte Name?
  idField: id              # Welches Feld ist die eindeutige ID?
  bildField: bild          # Welches Feld ist das Hauptbild?

# === FELDER ===
felder:
  id:
    typ: number
    versteckt: true
  
  titel:
    typ: string
    label: Titel
    suche:
      gewicht: 100
      exakt: true
  
  beschreibung:
    typ: string
    label: Beschreibung
    suche:
      gewicht: 20
  
  kategorie:
    typ: tag
    label: Kategorie
    suche:
      gewicht: 50
  
  tags:
    typ: list
    label: Tags
    suche:
      gewicht: 30
  
  bild:
    typ: image
    label: Bild

# === SEMANTISCHE SUCHE ===
semantik:
  # Beispiel: Suche nach "wichtig" findet Items mit kategorie=priorität
  wichtig:
    keywords:
      - wichtig
      - priorität
      - dringend
      - urgent
    feld: kategorie
    werte:
      - priorität
      - wichtig
    score: 50
  
  # Beispiel: Suche nach "neu" findet Items mit tag "neu"
  neu:
    keywords:
      - neu
      - aktuell
      - frisch
    feld: tags
    enthält:
      - neu
    score: 40

# === PERSPEKTIVEN ===
perspektiven:
  uebersicht:
    name: Übersicht
    symbol: 📋
    farbe: "#3b82f6"
    felder:
      - titel
      - kategorie
    keywords:
      - übersicht
      - liste
      - alle
  
  details:
    name: Details
    symbol: 🔍
    farbe: "#10b981"
    felder:
      - beschreibung
      - tags
    keywords:
      - detail
      - info
      - mehr
      - beschreibung
