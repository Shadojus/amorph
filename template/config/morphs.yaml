# Morph-Konfiguration
# Feld→Morph Mappings kommen aus schema.yaml/felder[].typ!

# Morph-spezifische Einstellungen
config:
  tag:
    farben:
      priorität: "#ef4444"
      normal: "#3b82f6"
      neu: "#10b981"
  
  text:
    maxLaenge: 200
  
  list:
    maxItems: 10
